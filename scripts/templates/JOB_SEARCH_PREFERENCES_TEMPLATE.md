# Job Search Preferences

What this person is actually looking for — separate from `facts_preferences_draft.md`'s resume-writing style preferences. Human-editable; the agent updates it conversationally via `/job-search-preferences` and the feedback loop in `/find-jobs`.

## Target Roles
- (title/role, plural — people often qualify for a spread, not one exact title)

## Location
- (remote-only / hybrid-ok / onsite-ok, and a local area for the "local-or-remote only" default filter — e.g. a city/region)

## Deal-breakers
- (e.g. no overnight shift, no >X% travel, no unpaid on-call)

## Compensation
- (optional — only if the person wants to share a floor)

## Industries / Company Types
- Seeking:
- Avoiding:

## Search Match Threshold
- (how wide a net — some people want only strong fits, some want to see anything plausible)

## Learned from Feedback
Grows over time via `/find-jobs`. Only ever added when the person explicitly confirms something as a standing rule — never inferred silently from behavior. Each entry should note *why* and *when* confirmed, e.g.:
- (none yet)
